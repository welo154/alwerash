# =============================================================================
# Database (Supabase)
# =============================================================================
# Pooled connection for runtime queries (use port 6543 with pgbouncer)
DATABASE_URL="postgresql://postgres.dvddvdrozdsfvuizycjv:Alwerash2026$@aws-1-eu-west-1.pooler.supabase.com:6543/postgres"

# Direct connection for migrations (use port 5432)
DIRECT_URL="postgresql://postgres:Alwerash2026$@db.dvddvdrozdsfvuizycjv.supabase.co:5432/postgres"

# Replace PROJECT_REF with your Supabase project ref, REGION with e.g. eu-west-1, YOUR-PASSWORD with the DB password.

# =============================================================================
# Auth.js / NextAuth
# =============================================================================
# Generate with: openssl rand -base64 32  (or on Windows: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
AUTH_SECRET="replace-with-generated-secret"

# Set to "true" in development; required for auth callbacks to work
AUTH_TRUST_HOST="true"

# App URL (use your real URL in production)
AUTH_URL="http://localhost:3000"

# Legacy names (Auth.js v4 / NextAuth); optional if you use AUTH_* above
# NEXTAUTH_SECRET="same-as-AUTH_SECRET"
# NEXTAUTH_URL="http://localhost:3000"

# =============================================================================
# Admin seed (for: npx prisma db seed)
# =============================================================================
ADMIN_EMAIL="barakat_welo@gmail.com"
ADMIN_PASSWORD="Alwerash2026$"
ADMIN_NAME="Platform Admin"
